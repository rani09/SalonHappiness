@model IEnumerable<SalonHappiness.Models.AboutUs>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h2>Om os</h2>
@if (Model.Count() == 0)
{
    <p>
        @Html.ActionLink("Opret Ny Om Os", "Create", null, new { @class = "btn btn-success" })
    </p>
}
<table class="table">
    <tr>
        <th>Billede</th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @if (item.AboutUsFiles.Any(f => f.FileType == SalonHappiness.Models.FileType.Avatar))
                {
                    <img src="~/AboutUsFile?id=@item.AboutUsFiles.First(f => f.FileType == SalonHappiness.Models.FileType.Avatar).AboutUsFileId" alt="avatar" width="75" height="85" />
                }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.ActionLink("Redigere", "Edit", new { id = item.AboutUsId }, new { @class = "btn btn-warning" }) |
                @Html.ActionLink("Detaljer", "Details", new { id = item.AboutUsId }, new { @class = "btn btn-info" }) |
                @Html.ActionLink("Slet ", "Delete", new { id = item.AboutUsId }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
